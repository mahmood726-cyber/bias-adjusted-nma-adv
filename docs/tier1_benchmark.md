<!-- sentinel:skip-file -->
# Tier-One Methods Benchmark

Checked: 2026-07-18

This note benchmarks the platform specification against mature specialist network meta-analysis methods. It is not evidence that the current Python implementation is superior to those methods.

## Bottom Line

The platform can plausibly become stronger than any single specialist package as an integrated evidence-synthesis environment. It should not claim that every native estimator beats the best specialist method in its domain until reference matching, simulation validation, and independent reproduction have been completed.

The correct claim is:

> Use tier-one methods as foundations and validation targets, then compete through integration, governance, diagnostics, reproducibility, and carefully validated methodological improvements.

The current blocker register is `validation/tier1_gap_register.toml`. It records three active blockers from multiperson review: feature completeness, numerical stability, and Bayesian ecosystem integration. While any of these remain `blocking`, the repository must not claim tier-one parity, tier-one superiority, production certification, clinical reporting, regulatory reporting, or HTA reporting.

The HTML delivery contract is `validation/html_delivery_contract.toml`. HTML dashboards are allowed for presenting and inspecting generated artifacts, but statistical estimation, live source verification, R/Stan/reference adapters, hash-producing validation, and CI/certification gates remain backend-required.

The current improvement review is `validation/reviews/improvement_review_2026_07_18.toml`. It records a four-role review of the tier-one blockers, source boundary, HTML delivery risk, and implementation polish. The review passes the current milestone, but explicitly does not mark the global objective complete.

The dose-response coverage check is `validation/dose_response_source_coverage.toml`. It now records one active source-backed CT.gov/PubMed smoke benchmark (`semaglutide_obesity_dose_response`) with dose amounts, units, source checks, a local weighted polynomial artifact, a narrow `metafor` fixed-effect polynomial reference-output validation, and a single-trial `MBNMAdose` linear-polynomial reference candidate. This is not dose-response NMA parity: multi-trial dose-response networks, shared-control covariance handling, broader functional families, and additional external `MBNMAdose` matching remain blocked.

The DTA coverage check is `validation/dta_source_coverage.toml`. It now records one source-backed open-access diagnostic test accuracy 2x2 benchmark (`midkine_elisa_cancer_dta`) from a PLOS JATS table, plus a narrow `mada::reitsma` source-table smoke reference. This closes the previous "no DTA source data" gap, but it is still not broad DTA validation: HSROC parity, threshold-effect diagnostics, multiple clinical domains, and wider reference matching remain blocked.

The ML-NMR coverage check is `validation/mlnmr_source_coverage.toml`, with source-search audits in `validation/mlnmr_source_search_2026_07_17.toml`, `validation/mlnmr_source_search_2026_07_18.toml`, and `validation/mlnmr_source_search_2026_07_18_diabetes.toml`. It records `missing_source_backed_mlnmr_data`: bounded local and web searches found reusable ML-NMR infrastructure, simulated IPD demos, synthetic `multinma` examples, survey-style public IPD registries, pseudo-IPD/KM-reconstruction assets, one legitimate single-trial lead (`NCT00950248` / PMID `32784117`), and one open-access diabetes IPD-NMA lead (PMID `40193407`, DOI `10.1371/journal.pmed.1004553`) whose row-level IPD remain controlled-access. None provides an admissible connected public trial-IPD plus source-bound aggregate covariate network. The coverage artifact now records a formal public-source exclusion for the real ML-NMR large-scale domain, but this is not validation evidence, not feature parity, and not `multinma` ML-NMR reference matching.

The real benchmark coverage atlas is `validation/real_benchmark_atlas.json`. It currently records 38 registered source-backed benchmark artifacts, 208 study-effect rows, 150 unique NCT IDs, and 144 unique PMIDs. That clears the current breadth thresholds in the large-scale validation gate, but the gate remains `partial_not_large_scale` because ML-NMR is formally excluded under the public-source boundary rather than validated, and no broad feature family is promoted to reference-matched status.

## Specialist Reference Targets

| Domain | Strong reference method or package | What must be matched first | Current platform position |
| --- | --- | --- | --- |
| Frequentist NMA | `netmeta` | Treatment effects, multi-arm covariance, inconsistency diagnostics, sparse-event handling, contribution matrices, disconnected-network warnings | Spec target plus local multi-arm fixture replay, a source-backed CT.gov star-network reference candidate, and one source-backed CT.gov closed-loop binary benchmark matching `netmeta` estimates; not broad parity |
| Bayesian NMA | `multinma` and validated CmdStan models | Posterior means, credible intervals, rank probabilities, diagnostics, prior sensitivity | Spec target plus source-backed CmdStan/NUTS and `multinma` SGLT2i reference candidates; not broad Bayesian parity |
| ML-NMR | `multinma` | IPD plus aggregate likelihood integration, population standardization, covariate-distribution checks | Spec target plus a fail-closed source-coverage gate; no real public trial-IPD plus aggregate covariate benchmark yet |
| Dose-response NMA | `MBNMAdose` | Functional dose-response families, class effects, dose extrapolation checks, consistency diagnostics | Spec target plus one CT.gov/PubMed source-verified smoke benchmark, a narrow `metafor` polynomial check, and one single-trial `MBNMAdose` linear-polynomial reference candidate; not broad `MBNMAdose` parity |
| Cross-design RCT/NRS synthesis | `crossnma` | Bayesian cross-format and cross-design models for RCT/NRS and IPD/aggregate mixtures | Spec target plus one source-backed separated-routing benchmark and a package-aware compatibility preflight that blocks the current HR fixture; not `crossnma` parity |
| Component NMA | `netmeta` additive CNMA and recent CNMA hierarchy workflows | Estimability checks, component additivity diagnostics, hierarchy constraints | Narrow algorithmic `netmeta::discomb` reference candidate plus one CT.gov/PubMed factorial smoke benchmark; not broad CNMA parity |
| Diagnostic test accuracy | `mada`, `metafor`, or validated bivariate/HSROC references | Source-backed TP/FP/FN/TN tables, bivariate random-effects sensitivity/specificity, HSROC, threshold effects | Experimental prototype plus one source-backed open-access table and narrow `mada::reitsma` source-table smoke reference; not broad DTA or HSROC parity |
| Evidence certainty | CINeMA and ROB-MEN | Contribution-aware certainty and missing-evidence assessment | Spec target plus local absolute contribution heatmap data; not CINeMA, ROB-MEN, or evidence-certainty parity |

## Where The Platform Can Beat Specialist Tools

1. Integration: one workflow can cover data validation, estimand definition, network checks, model fitting, bias analysis, registry comparison, certainty assessment, and reproducible reporting.
2. Governance: every module can carry an explicit certification level from Experimental through Production Certified.
3. Safety: complex models can be blocked when the data structure is disconnected, unidentified, sparse, or outside the model's support.
4. Bias-aware synthesis: probabilistic bias parameters, RCT/NRS separation, missing-evidence analysis, registry auditing, contribution matrices, and certainty reporting can be handled together rather than as disconnected post-processing.
5. Method routing: the platform can recommend the appropriate specialist class instead of blindly running the most complex model.

## Where It Cannot Claim Universal Superiority

There is no single estimator that is best under every network, outcome, and evidence-design structure.

Failure modes that must remain explicit:

- unstructured multivariate covariance can be worse than univariate analyses in small sparse networks;
- ML-NMR can mislead when aggregate covariate distributions are reconstructed incorrectly;
- CNMA can mislead when component additivity fails or disconnected networks depend on strong structural assumptions;
- dose-response models can improve precision while adding bias if the functional form is wrong;
- cross-design borrowing can degrade RCT estimates when observational bias is misspecified;
- narrower intervals are not proof of better inference.

## Staged Validation Strategy

The executable reference-target registry is `validation/reference_targets.toml`. It is intentionally conservative: every target starts as `planned`, and certification tests fail closed if a module is promoted without evidence artifacts. The grand-benchmark plan in `validation/grand_benchmark_plan.toml` separates real source-backed validation lanes from simulation-only operating-characteristic scenarios. Real-world effect evidence must come from AACT/ClinicalTrials.gov records, public numeric ICTRP/PACTR result rows, PubMed abstracts, open-access papers, or public FDA/EMA regulatory review rows with numeric per-trial provenance only. Protocol-only ICTRP, PACTR, and other registry rows may verify registration, planned outcomes, eligibility criteria, and dates, and may feed registered-primary-outcome and completeness-denominator ledgers, but cannot supply model-ready effects.

External reference adapters must also emit machine-readable run reports under `validation/reference_runs/`. A skipped or unavailable adapter is recorded as `certification_effect = "none"` and cannot count as reference-matching evidence. A report becomes a certification candidate only when it has `status = "passed"`, package versions, input and output artifact hashes, and a prespecified tolerance.

Real-data benchmarks must also be paired with source snapshots under `validation/source_checks/`. Source-identity snapshots verify that public ClinicalTrials.gov and PubMed records are reachable and match the manifest identifiers. Event-count and reported-HR token snapshots, when present, separately verify exact numeric tokens and nearby treatment terms in PubMed abstracts; they are still not a substitute for full open-access paper/table extraction, Kaplan-Meier digitization, or external reference-software parity. Source-check artifacts must carry `certification_effect = "none"`.

The canonical inventory for local source-backed real-data benchmarks is `validation/benchmark_registry.toml`, validated by `src/bias_nma_adv/benchmark_registry.py`. This registry pins artifact hashes, source-manifest hashes, source-check hashes, allowed evidence-source policy, source mode, required limitations, and `certification_effect = "none"`. It also revalidates source-check reports through their specialized schemas and checks that those reports reference the registered source manifests. A benchmark can be useful regression evidence while still being barred from tier-one parity or production certification until a separate external reference run passes.

Run `python scripts/validate_benchmark_registry.py --json` to execute this gate outside pytest and emit a machine-readable summary. A passing registry summary still reports `certification_effect = "none"`. Run `python scripts/write_real_benchmark_atlas.py` to regenerate the coverage atlas over registered real-data benchmark artifacts.

Run `python scripts/write_validation_status.py` to compose the source-backed benchmark registry, real benchmark atlas, reference-target registry, simulation-matrix metadata, and reference-adapter run reports into one machine-readable validation status report. The report is deliberately conservative: evidence-candidate reference-output reports do not change module certification status, and clinical/HTA reporting remains disabled when no module holds Production Certified status. Run `python scripts/run_simulation_matrix.py` to execute the non-certifying simulation smoke matrix. The same gates run in `.github/workflows/validation.yml`, which uploads the status JSON, real benchmark atlas, and simulation report as CI artifacts.

Current adapter preflights: `validation/reference_runs/pairwise_metafor_meta_preflight.toml`, `validation/reference_runs/multiarm_netmeta_preflight.toml`, `validation/reference_runs/dta_mada_preflight.toml`, `validation/reference_runs/stan_nuts_cmdstan_preflight.toml`, `validation/reference_runs/crossnma_sglt2_compatibility_preflight.toml`, and `validation/dependency_preflights/tier1_r_dependency_preflight.toml` record dependency availability or explicit fail-closed compatibility status. The local R environment now loads `multinma 0.9.1`, `MBNMAdose 0.5.1`, `rstan 2.32.7`, `rjags 4.17`, and `crossnma 1.3.1` when `JAGS_HOME` points at the locally extracted JAGS 4.3.2 runtime. These preflights do not certify parity adapters and remain `certification_effect = "none"`.

Current local R reference-output validations: `validation/reference_runs/pairwise_metafor_meta_reference.toml` validates the generated `metafor`/`meta` JSON output for study log-OR effects, fixed-effect pooling, REML tau2/Q/df, and the documented HKSJ floor difference. `validation/reference_runs/sglt2_hf_metafor_gosh_reference.toml` validates the same source-backed SGLT2i log-OR rows against `metafor::gosh` for all non-empty fixed-effect subset estimates and heterogeneity summaries. `validation/reference_runs/psoriasis_sparse_binary_metafor_reference.toml` validates one CT.gov/PubMed low-control-event psoriasis etanercept-versus-placebo binary contrast against `metafor::rma.uni` count-derived log-OR FE/DL/REML summaries; zero-event and Mantel-Haenszel parity remain unclaimed. `validation/reference_runs/breast_adjuvant_idfs_prediction_interval_metafor_reference.toml` validates one source-backed reported-HR prediction-interval convention against `metafor::predict` KNHA and separately checks the local conservative HKSJ floor against the benchmark. `validation/reference_runs/semaglutide_step_continuous_metafor_reference.toml` validates four CT.gov STEP adjusted treatment-difference rows against `metafor::rma.uni` and `meta::metagen` continuous mean-difference summaries. `validation/reference_runs/inclisiran_orion_continuous_metafor_reference.toml` validates three CT.gov ORION adjusted LDL-C treatment-difference rows against `metafor::rma.uni` and `meta::metagen` continuous mean-difference summaries. `validation/reference_runs/multinma_sglt2_binary_nma_reference.toml` validates one source-backed SGLT2i binary arm-count NMA against `multinma 0.9.1`/`rstan 2.32.7`, including posterior mean/SD alignment, R-hat, ESS, divergence, and treedepth checks. `validation/reference_runs/multiarm_netmeta_reference.toml` validates generated `netmeta` JSON output for the multi-arm fixed/random fixture estimates and standard errors. `validation/reference_runs/component_netmeta_cnma_reference.toml` validates the algorithmic component fixture against `netmeta::discomb`. `validation/reference_runs/dose_response_metafor_polynomial_reference.toml` validates the CT.gov/PubMed semaglutide dose-level smoke artifact against `metafor` fixed-effect through-zero linear and quadratic polynomial meta-regressions. `validation/reference_runs/mbnmadose_semaglutide_polynomial_reference.toml` validates the source-backed semaglutide arm-level LS mean table against `MBNMAdose 0.5.1`/`rjags 4.17` for one common-effect linear polynomial dose-response smoke run. `validation/reference_runs/sglt2_survival_hr_metafor_reference.toml`, `validation/reference_runs/pcsk9_survival_hr_metafor_reference.toml`, and `validation/reference_runs/sglt2_ckd_survival_hr_metafor_reference.toml` validate source-backed reported-HR fixed-effect log-HR pooling against `metafor`. `validation/reference_runs/t2d_ctgov_hr_network_netmeta_reference.toml` validates the source-backed CT.gov T2D MACE reported-HR star network against `netmeta` fixed-effect class estimates. `validation/reference_runs/publication_bias_t2d_ctgov_regtest_reference.toml` validates the same CT.gov reported-HR rows against `metafor::regtest` for one small-study-effect diagnostic. `validation/reference_runs/publication_bias_glp1_metafor_trimfill_reference.toml` validates one source-backed GLP-1 MACE reported-HR trim-and-fill sensitivity output against `metafor::trimfill` rows and fixed-effect summaries. `validation/reference_runs/psoriasis_pasi90_ctgov_binary_network_netmeta_reference.toml` validates one source-backed CT.gov/PubMed closed-loop binary multi-arm network against `netmeta` estimates and standard errors. `validation/reference_runs/psoriasis_pasi90_ctgov_binary_network_netsplit_reference.toml` validates the same source-backed closed-loop binary network against `netmeta::netsplit` back-calculated SIDE rows for one narrow inconsistency diagnostic. `validation/reference_runs/dta_mada_reitsma_midkine_source_reference.toml` validates one open-access Midkine ELISA 2x2 table against `mada::reitsma` for narrow bivariate DTA smoke parity. These reports are `evidence_candidate` artifacts for narrow reference matching checks only; the broad reference targets in `validation/reference_targets.toml` remain `planned`.

Current source-backed tau2 reference candidate: `validation/reference_runs/metafor_tau2_crosscheck_survival_reference.toml` validates FE, DerSimonian-Laird, Paule-Mandel, and REML pairwise summaries against `metafor::rma.uni` on SGLT2 HF, GLP-1 MACE, lipid cardiovascular outcomes, and hepatocellular carcinoma overall-survival reported-HR benchmarks. The tolerance explicitly allows small derived-I2 differences from optimizer tolerances and does not establish broad optimizer stress parity.

Current review ledger: `validation/reviews/multiperson_review_2026_07_18.toml` records source-boundary, statistical-methods, implementation-contract, and claims-governance review rounds. It is a process-control artifact only and carries `certification_effect = "none"`.

Current local multi-arm artifact: `validation/multiarm/netmeta_portfolio_multiarm_benchmark.toml` deterministically replays the governed arm-level fixture in `validation/multiarm/netmeta_portfolio_multiarm_arms.csv`, and `validation/reference_runs/multiarm_netmeta_reference.toml` validates generated `netmeta` output for that fixture. It is useful for regression testing multi-arm covariance handling, but it still carries no model certification and does not establish broad `netmeta` feature parity.

Current local multi-arm design capability: `diagnose_multiarm_design(...)` performs deterministic pre-fit checks for treatment connectivity, disconnected treatments, observed versus expected pairwise cliques in each study, design rank, and basic estimability. This supports fail-closed method routing and review before fitting, but it does not yet replace full mature-package design diagnostics across many real networks.

Current local multi-arm diagnostic capability: `fit_multiarm_gls(...)` returns row-level influence diagnostics for each fitted contrast row: observed value, fitted value, residual, GLS leverage, standardized residual, and an approximate Cook distance. It also reports an absolute GLS mapping-contribution diagnostic for each fitted non-reference treatment parameter, `MultiArmNMAFit.study_contribution_matrix()` aggregates those nonnegative proportions by study and target treatment, and `MultiArmNMAFit.study_contribution_heatmap()` exposes a renderer-ready matrix for HTML or report dashboards. The signed mapping coefficient is retained for auditability, but normalized contribution proportions are derived from absolute coefficients so raw signed algebra is not presented as evidence-flow percentages. These diagnostics help identify high-leverage, discordant, or highly mapped contrasts in the deterministic fixture tests, but they do not yet replace `netmeta`'s wider diagnostic ecosystem, CINeMA contribution matrices, netheat plots, or ROB-MEN contribution workflows and do not resolve the feature-completeness blocker.

Current local covariance-safety capability: the multi-arm GLS path now validates covariance matrices before inversion and fails closed when recovered multi-arm arm variances are incompatible with a positive-definite GLS fit. This addresses one narrow numerical-stability gap, but it does not replace optimizer stress testing, sparse-network convergence guards, local-minimum detection, or alternative heterogeneity-estimator cross-checks.

Current local deterministic failure-report capability: `report_multiarm_gls_fit(...)` returns a structured status report for multi-arm GLS attempts, including input, design, and fit-stage failures. This is useful for reproducible CI and review logs, but it is currently limited to the multi-arm GLS path and does not replace broader REML optimizer stress reporting.

Current local node-splitting capability: `fixed_effect_node_splitting(...)` performs deterministic fixed-effect direct-versus-indirect checks for observed treatment comparisons. It returns `not_estimable` when removing direct evidence disconnects the indirect network. `validation/reference_runs/psoriasis_pasi90_ctgov_binary_network_netsplit_reference.toml` adds one source-backed `netmeta::netsplit` back-calculation SIDE reference candidate on the CT.gov/PubMed psoriasis network. This is still a narrow inconsistency diagnostic and does not constitute broad node-splitting, netheat, design-by-treatment, or `multinma` inconsistency parity.

Current local small-study-effect capability: `egger_regression_diagnostic(...)` runs a deterministic Egger regression on reported study effects and standard errors. `validation/reference_runs/publication_bias_t2d_ctgov_regtest_reference.toml` now provides a narrow source-backed check against `metafor::regtest` on the CT.gov T2D MACE reported-HR rows. The output is diagnostic-only; it does not adjust treatment effects, does not prove publication bias, and does not replace trim-and-fill, ROB-MEN, or selection-model sensitivity workflows.

Current local outlier-space capability: `leave_one_out_outlier_diagnostic(...)` runs bounded pairwise leave-one-out refits and reports how much each omitted study changes the pooled estimate. `gosh_outlier_space_diagnostic(...)` performs exhaustive subset refits for small pairwise meta-analyses and fails closed when the requested subset space exceeds a prespecified cap. `validation/reference_runs/sglt2_hf_metafor_gosh_reference.toml` adds one source-backed `metafor::gosh` subset-space reference candidate on the SGLT2i heart-failure log-OR benchmark. These are deterministic screening tools only; they do not replace broad GOSH visualization parity, outlier-model fitting, or mature package review workflows.

Current local redescending sensitivity capability: `tukey_biweight_pairwise_sensitivity(...)` computes a deterministic Tukey-biweight pairwise sensitivity estimate and reports normalized study weights. `AdvancedBiasAdjustedNMAPooler(robust_outlier_sensitivity=True)` adds a default-off NMA-level Tukey-biweight contrast refit that reports contrast weights, iterations, and sensitivity warnings while preserving the default unadjusted path. These are outlier-sensitivity screens, not primary estimators, not fraud-containment certification, and not reference-matched robust-model parity.

Current local pairwise numerical-stability capability: `tau2_cross_check_report(...)` compares FE, DL, Paule-Mandel, and REML conventions, reports point-estimate sign changes, and reports when null-crossing status differs across estimators. `numerical_stress_report(...)` reports sparse-study, dominant-precision, and boundary-sensitive heterogeneity warnings. These are deterministic stress screens, not proof that the local optimizer is as hardened as `metafor` across sparse networks.

Current local REML optimizer diagnostic capability: `reml_local_minimum_diagnostic(...)` profiles the pairwise REML objective over a prespecified tau2 grid and reports the bounded optimizer solution, best grid point, local minima, boundary minima, and objective-gap warnings. `pairwise_numerical_stress_matrix(...)` runs these diagnostics across named stress scenarios. These outputs make optimizer risk auditable, but they are not reference-matched `metafor` optimizer parity.

Current local trim-and-fill capability: `trim_and_fill_sensitivity(...)` performs bounded mirror-imputation sensitivity analysis and reports the adjusted estimate alongside the observed estimate. `validation/reference_runs/publication_bias_glp1_metafor_trimfill_reference.toml` adds one source-backed `metafor::trimfill` sensitivity reference candidate on the GLP-1 MACE reported-HR benchmark. This still does not prove publication bias, does not provide ROB-MEN parity, and does not establish broad Duval-Tweedie trim-and-fill parity.

Current local selection-weight sensitivity capability: `selection_weight_sensitivity(...)` computes a prespecified inverse-selection-probability reweighted fixed-effect estimate for publication-bias tipping-point review. It requires explicit study-level probabilities in `(0, 1]`, reports the probabilities and inverse weights used, and warns that the output is a sensitivity analysis rather than a publication-bias proof or correction.

Current local sufficiency/fragility capability: `e_value_for_risk_ratio(...)`, `e_value_for_ci_limit(...)`, and `binary_event_fragility_index(...)` provide deterministic sensitivity summaries over already source-backed ratio estimates or event counts. They do not extract data, change the primary estimator, or certify causal robustness.

Current dose-response data coverage: `dose_response_source_coverage.toml` reports `active_source_backed_dose_response_data` for `semaglutide_obesity_dose_response`. The artifact uses CT.gov least-squares mean dose groups and PubMed identity verification to compute dose-level effects versus placebo, `dose_response_metafor_polynomial_reference.toml` checks the through-zero polynomial coefficients against `metafor`, and `mbnmadose_semaglutide_polynomial_reference.toml` checks one arm-level common-effect linear polynomial smoke run against `MBNMAdose`. It is still a single-trial smoke benchmark, ignores shared-placebo covariance, and is not broad `MBNMAdose` parity.

Recent Gemini panel feedback is treated as planning input: the repository has a defensible advantage in source provenance and HKSJ-floor safety defaults, but optimizer stability, feature completeness, and broad Bayesian/`multinma` parity remain blocking gaps until machine-verifiable artifacts demonstrate otherwise.

Current local Bayesian diagnostic capability: `BayesianNMAMCMCSampler.fit(...)` now exports local R-hat/ESS/MCSE diagnostic warnings through `compute_mcmc_diagnostics(...)`. Single-chain runs correctly report that R-hat is unavailable. This improves diagnostic transparency for the prototype sampler, but it does not provide Stan/NUTS sampling, divergence checks, treedepth checks, `multinma` matching, or production Bayesian certification.

Current local Bayesian predictive/ranking capability: `prior_predictive_check(...)`, `posterior_predictive_check(...)`, and `posterior_treatment_ranking(...)` provide deterministic prototype checks over declared priors and stored MCMC draws. Ranking preserves joint posterior draws, but these outputs are not Stan/NUTS, SUCRA, or `multinma` reference parity.

Current CmdStan preflight capability: `cmdstan_preflight(...)` reports whether CmdStanPy and CmdStan are available and lists the required NUTS diagnostics. Availability alone has `certification_effect = "none"`; skipped Stan parity is never treated as a pass.

Current Stan/NUTS and `multinma` reference gates: `external/stan/standard_binary_nma.stan` provides a minimal arm-level binary NMA model source, `validation/reference_runs/stan_nuts_cmdstan_preflight.toml` records backend availability, and `validation/reference_runs/stan_nuts_cmdstan_reference.toml` validates `validation/reference_runs/stan_nuts_cmdstan_output.json`. The source-backed CmdStan SGLT2i run has R-hat about 1.001, bulk/tail ESS above 2,300, no divergences, no treedepth saturation, posterior mean within 0.001 log-OR of the existing `metafor` fixed-effect reference, and same-model prior/posterior predictive summaries generated from declared priors and posterior `y_rep` counts. `external/r/multinma_sglt2_binary_nma.R` now runs the same source-backed SGLT2i arm-count dataset through `multinma`/`rstan`; `validation/reference_runs/multinma_sglt2_binary_nma_reference.toml` records posterior mean difference 0.00034, R-hat <= 1.01, ESS >= 400, zero divergences, and treedepth <= 10. These are narrow evidence candidates only; they do not establish ML-NMR, ranking, inconsistency, or broad Bayesian parity.

Current reported-HR survival reference checks: `validation/reference_runs/sglt2_survival_hr_metafor_reference.toml`, `validation/reference_runs/pcsk9_survival_hr_metafor_reference.toml`, and `validation/reference_runs/sglt2_ckd_survival_hr_metafor_reference.toml` validate the source-backed PubMed/CT.gov reported-HR benchmark artifacts against `metafor` fixed-effect log-HR pooling. The newer `validation/survival/glp1_mace_reported_hr_benchmark.toml`, `validation/survival/parp_firstline_ovarian_pfs_reported_hr_benchmark.toml`, and `validation/survival/parp_recurrent_ovarian_pfs_reported_hr_benchmark.toml` add PubMed/CT.gov reported-HR artifacts without external reference status in this checkpoint. The CKD, GLP-1, and first-line PARP artifacts supply three positive-`tau2` source-backed heterogeneity-stress signals. These checks do not digitize Kaplan-Meier curves, do not run a survival NMA, and do not satisfy the `survival_km_oa_ipd_reconstruction` target.

Current transportability support capability: `src/bias_nma_adv/transportability.py` implements exploratory meta-regression transport, non-collapsibility guards for OR/logOR, and a topology-aware support certificate adapted from `topo-transport-ma` and re-tested locally. The donut and subpopulation tests demonstrate the diagnostic mechanics, not clinical transportability validity; the `transportability_support_topology` reference target remains planned.

Current KM reconstruction guard: `validation/survival/km_reconstruction_policy.toml` and `src/bias_nma_adv/km_reconstruction.py` define the survival lane's first fail-closed screen and curve-fidelity metric layer. Open-access paper figure provenance is required, text-only HR outputs are rejected, synthetic IPD fallbacks are blocked, and the native Python reconstruction path now uses a faithful Guyot-style loop that iteratively aligns censoring to number-at-risk anchors, conserves the total population, and can reconcile source-reported event totals without adding or dropping records. The resulting KM fidelity report is non-certifying and still cannot substitute for OA figure/source validation.

Current sparse cross-design guard: `sparse_design_bias_diagnostics(...)` in `src/bias_nma_adv/ctgov_hr_network.py` flags treatment nodes where RCT/NRS design-bias terms are locally underidentified. It recommends separated reporting or hierarchical shrinkage before cross-design borrowing, and it is not a `crossnma` parity model.

Current source-backed cross-design coverage: `validation/cross_design/sglt2_rct_nrs_cross_design_benchmark.toml` records two PubMed-verified SGLT2 inhibitor RCT rows and two PubMed-verified CVD-REAL observational rows. The benchmark computes separated RCT and NRS fixed-effect summaries and reports `separated_only_estimand_mismatch`, blocking combined borrowing because comparator, target population, and outcome definitions differ. `validation/reference_runs/crossnma_sglt2_compatibility_preflight.toml` additionally loads `crossnma 1.3.1`, verifies the source-backed rows, and records an expected failed preflight because the current fixture is reported-HR contrast data rather than crossnma-compatible arm-level OR/RR/MD/SMD data. This removes the previous no-real-cross-design-domain blocker and adds a package-aware compatibility gate, but it is not `crossnma` reference matching and does not certify cross-design borrowing.

Current DTA coverage: `validation/dta_source_coverage.toml` reports `active_source_backed_dta_data` with `registered_benchmark_ids = ["midkine_elisa_cancer_dta"]`. `src/bias_nma_adv/dta.py` implements an experimental bivariate logit-normal REML prototype, `validation/reference_runs/dta_mada_reitsma_reference.toml` validates a nonclinical algorithmic fixture against `mada::reitsma`, and `validation/reference_runs/dta_mada_reitsma_midkine_source_reference.toml` validates one source-backed open-access 2x2 table. DTA remains source-eligible only when TP/FP/FN/TN tables, index-test labels, reference standards, and thresholds are source-bound to CT.gov, public numeric ICTRP/PACTR result rows, PubMed abstracts, open-access papers, or public FDA/EMA regulatory review rows. Broad DTA real-data validation, HSROC parity, and diagnostic-accuracy superiority claims remain blocked.

Current ML-NMR coverage: `validation/mlnmr_source_coverage.toml` reports `missing_source_backed_mlnmr_data` and `model_status = "simulated_reference_only"`. The 2026-07-17 and 2026-07-18 source-search audits record excluded candidates, including local synthetic examples, public `multinma` examples that use simulated/synthetic IPD or reconstructed inputs, WorldIPD-style availability caches, Wasserstein/KM reconstruction assets, `NCT00950248` / PMID `32784117` as a valid single-trial IPPoMS lead that is not a connected ML-NMR network, and PMID `40193407` / DOI `10.1371/journal.pmed.1004553` as an open-access diabetes IPD-NMA lead whose row-level IPD remain controlled-access through sponsor/Vivli workflows. The required source components remain public trial IPD rows, source-bound aggregate covariate distributions, a shared estimand/treatment/outcome map, covariate-overlap diagnostics, and a versioned `multinma` ML-NMR reference output. Simulated `multinma` vignette IPD, synthetic cohorts, pseudo-IPD reconstructed from KM curves, survey-only public IPD, single-trial leads, controlled-access IPD, and closed patient datasets are explicitly excluded from real ML-NMR validation. This formalizes the ML-NMR source-boundary exclusion while keeping ML-NMR feature parity and clinical population-adjustment claims blocked until an admissible public-source fixture is found and reference matched.

Current component-NMA coverage: `src/bias_nma_adv/component_nma.py` implements a narrow fixed-effect additive component core with rank and estimability checks. `external/r/component_netmeta_cnma_fixture.R` validates the same algorithmic contrast fixture against `netmeta::discomb` through `validation/reference_runs/component_netmeta_cnma_reference.toml`. `validation/component/sitagliptin_pioglitazone_component_source_benchmark.toml` adds one source-backed CT.gov/PubMed factorial smoke benchmark using verified arm-level A1C least-squares means from NCT00722371 and PMID 23909985. This removes the "no component real-data domain" blocker, but it does not certify CNMA feature parity, ignores same-trial arm covariance, and remains far below broad source-backed component-network validation.

Current proof-carrying ingestion bundle: `validation/ingestion/sglt2_hf_reported_hr_proof_effects.json` records four SGLT2 reported-HR effects as source-backed extracted-effect rows. The bundle is regenerated by `scripts/write_proof_effect_bundle.py`, verifies source-manifest/report hashes, validates PubMed abstract snippets containing the HR and CI tokens, and still carries `certification_effect = "none"` because source provenance does not prove model performance or tier-one parity.

Current reversal answer-key pin gate: `validation/reversal_yardstick.toml` records SHA-256 pins for external answer-key files without committing private local paths. `scripts/verify_reversal_source_artifacts.py --source-artifact fix_md=<path>` recomputes supplied pins and fails closed on hash drift; missing external files are reported distinctly as `unavailable` unless the caller explicitly allows that state. `scripts/run_reversal_yardstick.py --arena-json <path>` now verifies the pinned external aggregate `arena.json` against the committed yardstick metrics and reports `aggregate_verified_not_source_backed`. This makes FIX.md and arena drift detectable without making the public repository depend on a local `C:\key` path. It still does not fit source-backed reversal cases, complete negative controls, or support superiority claims.

Current input-source lane capability: `StudyRecord.source_type`, `OutcomeADRecord.source_type`, and `EvidenceDataset.subset_by_input_source(...)` preserve provenance after ingestion so the same network can be fit as `as_published` or `registry_first` before the estimator runs. Untagged rows are excluded from both lanes, protocol-only sources are rejected as model-ready effects, and estimator flags remain unchanged.

Current RapidMeta import capability: `evidence_dataset_from_rapidmeta_app_index(...)` consumes only the strict `rapidmeta_app_index/v1` JSON contract with binary arm-level counts and allowed effect-source types. It rejects ambiguous multi-analysis exports unless `analysis_id` is specified and does not infer evidence from private or undocumented app schemas.

Current real network artifacts: `validation/networks/t2d_mace_ctgov_hr_network_benchmark.toml` recomputes a ClinicalTrials.gov-reported HR star network for type 2 diabetes cardiovascular outcome trials from `validation/networks/t2d_mace_ctgov_hrs.toml` and `validation/source_checks/t2d_mace_ctgov_hr_network_check.json`; `validation/reference_runs/t2d_ctgov_hr_network_netmeta_reference.toml` matches its fixed-effect class estimates against `netmeta`. `validation/networks/psoriasis_pasi90_ctgov_binary_network_benchmark.toml` recomputes a CT.gov arm-count PASI 90 closed-loop binary network from FIXTURE and UNCOVER-3; `validation/reference_runs/psoriasis_pasi90_ctgov_binary_network_netmeta_reference.toml` matches its multi-arm estimates and standard errors against `netmeta`, and `validation/reference_runs/psoriasis_pasi90_ctgov_binary_network_netsplit_reference.toml` matches one `netmeta::netsplit` SIDE table. These are still narrow source-governed regression tests, not broad NMA parity: the T2D network is placebo-centered and class-labelled, while the psoriasis network does not yet provide broad inconsistency diagnostics.

Current estimator disclosure guard: `AdvancedNMAFitResult.tau_method` records whether the local NMA path used `common_effect`, `REML`, `REML_stratified`, `fixed_zero_insufficient_df`, or `exact_binomial_no_tau`. The model also fails closed on mixed outcome measure types and reports `n_studies_dropped` plus warnings for unusable study rows. These are audit safeguards; they do not turn the local optimizer into a reference-matched tier-one implementation.

Current feature-parity gate: `validation/feature_parity_matrix.toml` records 12 feature families across pairwise meta-analysis, frequentist NMA, inconsistency, publication bias, Bayesian NMA, ML-NMR, dose-response, cross-design, component NMA, DTA, and large-scale validation. It currently contains reference candidates and local implementations, including a narrow `MBNMAdose` dose-response candidate, a narrow `metafor::regtest` publication-bias diagnostic candidate, a narrow `netmeta::netsplit` inconsistency candidate, and local heatmap-ready absolute GLS contribution matrices, but no `reference_matched` feature-family status.

Current large-scale validation gate: `validation/large_scale_validation.toml` dynamically compares the current benchmark atlas, simulation matrix, executed full simulation report, and reference reports against prespecified thresholds. The current status is `partial_not_large_scale`: 38 source-backed benchmarks, 208 study-effect rows, 150 unique NCT IDs, 144 unique PMIDs, 23 passed reference reports, 24 source-backed benchmarks with positive `tau2`, 25 passed full simulation jobs, and 10,588 successful full simulation iterations exist. The simulation report is operating-characteristic evidence only; it is not real clinical validation or tier-one superiority evidence. The benchmark-count, row-count, NCT, PMID, tau-positive, reference-report, simulation-job, and simulation-iteration thresholds are now met; the remaining large-scale blocker is a formal, non-certifying ML-NMR source-boundary exclusion rather than a validated real ML-NMR domain.

### Phase 1: Match Tier One

For each module, reproduce the established implementation before claiming improvement.

Acceptance criteria:

- deterministic estimates match within a prespecified numerical tolerance;
- Bayesian posterior differences are no larger than expected Monte Carlo error;
- multi-arm covariance handling is equivalent or deliberately documented;
- disconnected and unidentified networks produce equivalent warnings or fail-closed behavior;
- differences from reference software are documented as intentional, tested, and clinically interpretable.

### Phase 2: Beat Them Operationally

After reference matching, compete through:

- automatic schema recognition;
- pre-fit estimand checks;
- model-routing diagnostics;
- fail-closed data validation;
- parallel Bayesian and frequentist sensitivity analyses;
- reproducible execution bundles;
- contribution-based certainty assessment;
- registry and missing-evidence integration.

### Phase 3: Beat Selected Methods Statistically

Only after phases 1 and 2 should native methodological improvements be claimed. Candidate improvements must be tested prospectively against reference methods across prespecified simulations for:

- bias;
- RMSE;
- interval coverage;
- calibration;
- convergence failure;
- runtime;
- robustness to misspecification.

## Grand Benchmark Design

A credible superiority benchmark should include thousands of scenarios across:

- binary, continuous, count, ordinal, multinomial, and survival outcomes;
- small and large networks;
- multi-arm trials;
- rare and zero events;
- low and high heterogeneity;
- coherent and incoherent networks;
- IPD plus aggregate evidence;
- effect-modifier imbalance;
- disconnected networks;
- multicomponent treatments;
- dose-response misspecification;
- RCT/NRS conflicts;
- missing outcomes and publication bias.

Winners should be reported separately for statistical accuracy, uncertainty calibration, robustness, runtime, diagnostic quality, reproducibility, and user safety.

## Static-Vs-Dynamic Hardcode Disclosure

| Item | Static or dynamic | Evidence source | Disclosure |
| --- | --- | --- | --- |
| Reference package list | Static | Public package documentation checked on 2026-07-15 | Used as validation targets, not embedded computation |
| Package versions | Dynamic | CRAN or package site metadata checked on 2026-07-15 | May drift and must be refreshed before release claims |
| Platform capability status | Static repository review | Current repository files and tests checked on 2026-07-15 | Current implementation is not certified as tier-one parity |
| Benchmark thresholds | Static specification | This document and `docs/technical_specification.md` | Must be implemented as machine-readable validation criteria before certification |
| Reference-run preflight | Dynamic local environment check | `validation/reference_runs/*_preflight.toml` | Failed or unavailable preflights are recorded but cannot certify a module |
| R reference-output validation | Dynamic local R execution plus Python output comparison | `validation/reference_runs/*_output.json`, `validation/reference_runs/*_reference.toml`, `src/bias_nma_adv/r_reference_validation.py`, and `tests/test_r_reference_validation.py` | Validates narrow local parity artifacts with package versions, output hashes, and tolerance; does not promote broad targets or enable clinical/HTA reporting |
| Multi-arm fixture replay | Static algorithmic fixture plus dynamic local recomputation | `validation/multiarm/*` and `tests/test_multiarm_artifact.py` | Validates local covariance handling on a small fixture; not clinical evidence and not external `netmeta` parity |
| Multi-arm pre-fit design diagnostic | Dynamic deterministic design-matrix and clique checks | `src/bias_nma_adv/multiarm.py`, `tests/test_multiarm.py`, and `validation/tier1_gap_register.toml` | Adds connectivity, disconnected-treatment, complete-clique, design-rank, and estimability checks; not full mature-package multi-arm design parity |
| Multi-arm influence diagnostics | Dynamic deterministic GLS calculation on fitted contrast rows | `src/bias_nma_adv/multiarm.py`, `tests/test_multiarm.py`, and `validation/tier1_gap_register.toml` | Adds leverage, residual, standardized residual, and approximate Cook-distance diagnostics; this is partial feature-completeness progress, not tier-one parity |
| Multi-arm mapping contributions | Dynamic deterministic GLS mapping calculation on fitted contrast rows | `src/bias_nma_adv/multiarm.py`, `tests/test_multiarm.py`, and `validation/tier1_gap_register.toml` | Adds nonnegative absolute mapping-contribution proportions per fitted non-reference treatment; not a full netheat, CINeMA, or ROB-MEN contribution matrix |
| Multi-arm study contribution matrix | Dynamic aggregation of fitted-row contribution diagnostics | `src/bias_nma_adv/multiarm.py`, `tests/test_multiarm.py`, and `validation/tier1_gap_register.toml` | Aggregates nonnegative study-level contribution proportions by fitted target treatment; still not netheat, CINeMA, ROB-MEN, or clinical certainty parity |
| Multi-arm contribution heatmap data | Dynamic renderer-ready aggregation of study contribution values | `src/bias_nma_adv/multiarm.py`, `tests/test_multiarm.py`, and `validation/tier1_gap_register.toml` | Provides deterministic matrix values for dashboards; still not netheat visualization parity or evidence-certainty certification |
| Multi-arm covariance fail-closed guard | Dynamic matrix validation before GLS inversion | `src/bias_nma_adv/multiarm.py`, `tests/test_multiarm.py`, and `validation/tier1_gap_register.toml` | Rejects incompatible multi-arm covariance inputs instead of repairing or silently inverting invalid matrices; broader numerical stress testing remains outstanding |
| Multi-arm deterministic fit report | Dynamic structured pass/fail report around the multi-arm GLS path | `src/bias_nma_adv/multiarm.py`, `tests/test_multiarm.py`, and `validation/tier1_gap_register.toml` | Records input, design, and fit-stage failures without raising; broader optimizer failure-reporting remains outstanding |
| Fixed-effect node splitting | Dynamic deterministic direct-vs-indirect contrast calculation plus one `netmeta::netsplit` reference-output check | `src/bias_nma_adv/node_splitting.py`, `external/r/netmeta_netsplit_psoriasis.R`, `validation/reference_runs/psoriasis_pasi90_ctgov_binary_network_netsplit_reference.toml`, `tests/test_node_splitting.py`, and `tests/test_r_reference_validation.py` | Adds closed-loop inconsistency smoke diagnostics and one source-backed SIDE reference candidate; not full node-splitting, netheat, or design-by-treatment parity |
| Pairwise leave-one-out outlier-space screen | Dynamic deterministic pairwise refits | `src/bias_nma_adv/pairwise.py`, `tests/test_pairwise.py`, and `validation/tier1_gap_register.toml` | Identifies influential single-study omissions; not full GOSH subset enumeration or visualization parity |
| Pairwise exhaustive GOSH-style subset screen | Dynamic deterministic pairwise subset refits with a fail-closed cap plus one source-backed R output check | `src/bias_nma_adv/pairwise.py`, `external/r/metafor_gosh_sglt2.R`, `validation/reference_runs/sglt2_hf_metafor_gosh_output.json`, `validation/reference_runs/sglt2_hf_metafor_gosh_reference.toml`, `tests/test_pairwise.py`, and `validation/tier1_gap_register.toml` | Enumerates bounded subset spaces for small meta-analyses and matches one `metafor::gosh` fixed-effect subset space; not broad GOSH visualization parity or an outlier-model correction |
| Source-backed continuous pairwise reference | Dynamic CT.gov API extraction plus local R validation | `scripts/build_semaglutide_step_continuous_ctgov.py`, `scripts/build_inclisiran_orion_continuous_ctgov.py`, `external/r/metafor_continuous_semaglutide_step.R`, `external/r/metafor_continuous_inclisiran_orion.R`, `validation/continuous/*_ctgov_benchmark.toml`, `validation/reference_runs/*_continuous_metafor_reference.toml`, and `tests/test_r_reference_validation.py` | Validates seven CT.gov adjusted treatment-difference rows across STEP body-weight and ORION LDL-C domains against `metafor` and `meta` continuous mean-difference summaries; both local artifacts expose positive REML `tau2` for heterogeneity-stress accounting; not broad continuous-outcome parity |
| Pairwise and NMA redescending sensitivity | Dynamic deterministic Tukey-biweight reweighting over one contrast or default-off NMA contrast refit | `src/bias_nma_adv/redescending.py`, `src/bias_nma_adv/model.py`, `tests/test_redescending.py`, `tests/test_model.py`, and `validation/tier1_gap_register.toml` | Reports robust sensitivity estimates and weights; not a primary estimator, not fraud-containment certification, and not reference-matched robust-model parity |
| Pairwise tau2 cross-check and stress report | Dynamic deterministic pairwise method comparisons | `src/bias_nma_adv/pairwise.py`, `tests/test_pairwise.py`, and `validation/tier1_gap_register.toml` | Compares FE/DL/PM/REML and flags sparse or dominant-study configurations; not broad optimizer robustness parity |
| Source-backed tau2 cross-check reference | Dynamic local R execution plus Python validation | `external/r/metafor_tau2_crosscheck_survival.R`, `validation/reference_runs/metafor_tau2_crosscheck_survival_output.json`, `validation/reference_runs/metafor_tau2_crosscheck_survival_reference.toml`, and `src/bias_nma_adv/r_reference_validation.py` | Validates FE/DL/PM/REML pairwise summaries across four source-backed reported-HR benchmarks; not broad optimizer stress parity |
| Pairwise REML profile and stress matrix | Dynamic deterministic tau2-objective profiling and named scenario stress reports | `src/bias_nma_adv/pairwise.py`, `tests/test_pairwise.py`, and `validation/tier1_gap_register.toml` | Exposes boundary/local-minimum risk and scenario failures; not reference-matched optimizer robustness parity |
| Bounded trim-and-fill sensitivity | Dynamic deterministic mirror-imputation sensitivity plus one source-backed R output check | `src/bias_nma_adv/pairwise.py`, `external/r/publication_bias_metafor_trimfill_glp1.R`, `validation/reference_runs/publication_bias_glp1_metafor_trimfill_output.json`, `validation/reference_runs/publication_bias_glp1_metafor_trimfill_reference.toml`, `tests/test_pairwise.py`, and `validation/tier1_gap_register.toml` | Reports local sensitivity output and validates one `metafor::trimfill` source-backed GLP-1 sensitivity run; not broad Duval-Tweedie trim-and-fill, ROB-MEN, or selection-model parity |
| Egger small-study-effect diagnostic | Dynamic deterministic regression on reported effects and standard errors | `src/bias_nma_adv/publication_bias.py`, `tests/test_publication_bias_diagnostics.py`, and `validation/tier1_gap_register.toml` | Adds an asymmetry signal with underpowered-study warnings; not a treatment-effect correction, trim-and-fill analysis, or publication-bias proof |
| Selection-weight publication-bias sensitivity | Dynamic deterministic reweighting from explicit user-supplied selection probabilities | `src/bias_nma_adv/publication_bias.py`, `tests/test_publication_bias_diagnostics.py`, and `validation/tier1_gap_register.toml` | Supports tipping-point review; probabilities are not inferred and outputs do not prove or correct publication bias |
| E-value and binary fragility sensitivity | Dynamic deterministic transformation of source-backed effects or counts | `src/bias_nma_adv/sufficiency_fragility.py`, `tests/test_sufficiency_fragility.py`, and `validation/tier1_gap_register.toml` | Reports sensitivity quantities without creating evidence, changing the estimator, or proving causal robustness |
| Local Bayesian MCMC diagnostics | Dynamic deterministic diagnostics over posterior draws | `src/bias_nma_adv/bayesian.py`, `tests/test_bayesian.py`, and `validation/tier1_gap_register.toml` | Adds R-hat/ESS/MCSE warning exports for prototype sampler outputs; not Stan/NUTS or `multinma` parity |
| Local Bayesian predictive and ranking checks | Dynamic deterministic predictive simulation and draw-preserving treatment ranks | `src/bias_nma_adv/bayesian.py`, `tests/test_bayesian.py`, and `validation/tier1_gap_register.toml` | Adds prior/posterior predictive checks and joint-draw rank probabilities for prototype output; not Stan/NUTS, SUCRA, or `multinma` parity |
| CmdStan preflight contract | Dynamic environment preflight | `src/bias_nma_adv/stan_backend.py`, `tests/test_stan_backend.py`, and `validation/tier1_gap_register.toml` | Fails closed when CmdStanPy/CmdStan is unavailable and records required NUTS diagnostics; availability alone is not a model run or reference match |
| Stan/NUTS and `multinma` reference candidates | Dynamic source-backed CmdStan and `multinma` runs plus static artifact validation | `external/stan/standard_binary_nma.stan`, `external/r/multinma_sglt2_binary_nma.R`, `scripts/run_stan_nuts_reference.py`, `validation/reference_runs/stan_nuts_cmdstan_output.json`, `validation/reference_runs/stan_nuts_cmdstan_reference.toml`, `validation/reference_runs/multinma_sglt2_binary_nma_output.json`, `validation/reference_runs/multinma_sglt2_binary_nma_reference.toml`, `src/bias_nma_adv/stan_reference_validation.py`, `src/bias_nma_adv/r_reference_validation.py`, and tests | Validates one source-backed SGLT2i arm-level binary NMA with NUTS diagnostics, same-model prior/posterior predictive summaries, and fixed-effect mean alignment under both CmdStan and `multinma`; not broad ML-NMR, ranking, inconsistency, or Bayesian production parity |
| KM reconstruction policy and metrics | Static screen plus local unit fixtures | `validation/survival/km_reconstruction_policy.toml`, `src/bias_nma_adv/km_reconstruction.py`, `src/bias_nma_adv/survival.py`, and `tests/test_km_reconstruction_policy.py` | Blocks text-only and synthetic-IPD survival outputs, runs a native faithful Guyot-style reconstruction loop with population conservation and risk-anchor tests, and computes non-certifying KM curve-fidelity metrics; does not digitize any OA curve yet |
| CT.gov reported-HR network | Static manifest plus dynamic CT.gov source snapshot | `validation/networks/t2d_mace_ctgov_hrs.toml`, `validation/source_checks/t2d_mace_ctgov_hr_network_check.json`, and `tests/test_ctgov_hr_network.py` | Verifies public CT.gov HR/CI records and recomputes a local star-network GLS artifact; narrow `netmeta` fixed-effect class reference exists, but this is not broad NMA parity or clinical guidance |
| CT.gov closed-loop binary NMA benchmark | Dynamic CT.gov arm-count plus PubMed identity source check | `validation/networks/psoriasis_pasi90_ctgov_binary_network.toml`, `validation/networks/psoriasis_pasi90_ctgov_binary_network_benchmark.toml`, `validation/reference_runs/psoriasis_pasi90_ctgov_binary_network_netmeta_reference.toml`, `validation/reference_runs/psoriasis_pasi90_ctgov_binary_network_netsplit_reference.toml`, `src/bias_nma_adv/ctgov_binary_network.py`, and `tests/test_ctgov_binary_network.py` | Adds one source-backed closed-loop binary multi-arm network with `netmeta` estimate/SE matching and one `netsplit` SIDE candidate; broad inconsistency parity remains blocked |
| Proof-carrying ingestion contract | Static source-row/effect-row contract plus unit fixtures | `src/bias_nma_adv/ingestion.py` and `tests/test_ingestion.py` | Requires source-backed identifiers, snippets, uncertainty, and ratio-scale sanity before extracted effects can enter models; not numeric extraction validation |
| Input source lane switch | Dynamic pre-estimator dataset subset over source types | `src/bias_nma_adv/data.py`, `src/bias_nma_adv/evidence_sources.py`, and `tests/test_model.py` | Separates `as_published` from `registry_first` rows before fitting; untagged/protocol-only rows do not enter either model-ready lane |
| RapidMeta app-index adapter contract | Dynamic fail-closed import from a strict public JSON contract | `src/bias_nma_adv/rapidmeta_adapter.py` and `tests/test_rapidmeta_adapter.py` | Imports only validated binary arm-level counts with allowed effect sources; does not guess private app schemas |
| Registry/regulatory/protocol boundary | Static source-policy contract plus unit fixtures | `src/bias_nma_adv/source_type_policy.toml`, `src/bias_nma_adv/evidence_sources.py`, `src/bias_nma_adv/ingestion.py`, `validation/grand_benchmark_plan.toml`, and source-boundary tests | Allows AACT/ClinicalTrials.gov, public numeric ICTRP/PACTR result rows, and public FDA/EMA regulatory review rows as effect sources when numeric per-trial provenance is present; protocol-only registry rows are metadata ledgers only and are rejected as model-ready effect evidence |
| Study-design policy | Static config plus unit fixtures | `src/bias_nma_adv/study_design_policy.toml`, `src/bias_nma_adv/data.py`, and `tests/test_model.py` | Moves design/frame labels out of a hardcoded 3-way enum while blocking metadata-only `registry_only` rows from carrying model-ready arms or outcomes |
| Proof-carrying reported-HR bundle | Dynamic PubMed abstract read plus static manifest/report hash checks | `validation/ingestion/sglt2_hf_reported_hr_proof_effects.json`, `scripts/write_proof_effect_bundle.py`, and `tests/test_proof_effect_bundle.py` | Validates four SGLT2 reported HR extracted-effect rows against source manifests, source-check hashes, and minimal HR/CI snippets; not model-performance certification |
| Source-backed benchmark registry | Static registry plus dynamic hash checks | `validation/benchmark_registry.toml` and `tests/test_benchmark_registry.py` | Provides the canonical inventory of local real-data benchmark artifacts and enforces non-certification status until external reference artifacts pass |
| Real benchmark coverage atlas | Dynamic registry-derived summary | `validation/real_benchmark_atlas.json`, `src/bias_nma_adv/real_benchmark_atlas.py`, and `tests/test_real_benchmark_atlas.py` | Summarizes registered real-data benchmark coverage, source-check scopes, and non-claims; it is not reference matching or clinical certification |
| Grand benchmark plan | Static source-bound plan plus dynamic registry cross-check | `validation/grand_benchmark_plan.toml`, `src/bias_nma_adv/grand_benchmark_plan.py`, and `tests/test_grand_benchmark_plan.py` | Separates real source-backed lanes from simulation scenarios; contains no results and cannot certify superiority |
| Tier-one gap register | Static blocker register plus validator | `validation/tier1_gap_register.toml`, `src/bias_nma_adv/tier1_gap_register.py`, and `tests/test_tier1_gap_register.py` | Keeps feature completeness, numerical stability, and Bayesian ecosystem limitations as active blockers until machine-verifiable promotion evidence exists |
| Feature-parity matrix | Static fine-grained parity ledger plus validator | `validation/feature_parity_matrix.toml`, `src/bias_nma_adv/feature_parity_matrix.py`, and `tests/test_feature_parity_matrix.py` | Tracks feature-family status below the coarse blocker register; no family is currently broad-reference-matched |
| Large-scale validation gate | Dynamic threshold check over benchmark, simulation, and reference evidence | `validation/large_scale_validation.toml`, `src/bias_nma_adv/large_scale_validation.py`, and `tests/test_large_scale_validation.py` | Reports current validation evidence as partial and blocks large-scale validation claims until prespecified thresholds pass |
| HTML delivery contract | Static delivery contract plus validator | `validation/html_delivery_contract.toml`, `src/bias_nma_adv/html_delivery_contract.py`, and `tests/test_html_delivery_contract.py` | Allows HTML dashboards for read-only artifact inspection but blocks HTML-only replacement of engines, source verification, adapters, hashes, and certification gates |
| CT.gov sparse design-bias guard | Dynamic pre-fit treatment-by-design counts | `src/bias_nma_adv/ctgov_hr_network.py` and `tests/test_ctgov_hr_network.py` | Flags one-RCT/one-NRS style underidentification before cross-design borrowing; not a hierarchical Bayesian cross-design model |
| Source-backed cross-design routing benchmark | Dynamic PubMed-token-verified RCT/NRS fixture plus package-aware compatibility preflight | `validation/cross_design/sglt2_rct_nrs_cross_design_benchmark.toml`, `validation/reference_runs/crossnma_sglt2_compatibility_preflight.toml`, `src/bias_nma_adv/cross_design_benchmark.py`, `scripts/verify_cross_design_sources.py`, and tests | Separates RCT and NRS summaries, loads `crossnma`, and blocks model execution for log-HR effect-scale and estimand incompatibility; not `crossnma` reference matching |
| Dose-response source coverage and smoke benchmark | Static source-coverage check plus source-verified local artifact and dynamic R references | `validation/dose_response_source_coverage.toml`, `validation/dose_response/`, `validation/reference_runs/mbnmadose_semaglutide_polynomial_reference.toml`, `src/bias_nma_adv/dose_response_benchmark.py`, and tests | Registers one CT.gov/PubMed semaglutide dose-response smoke benchmark and one `MBNMAdose` linear-polynomial reference candidate; blocks broad `MBNMAdose` parity and dose-response superiority claims |
| Component-NMA source benchmark | Static manifest plus dynamic CT.gov/PubMed source check | `validation/component/sitagliptin_pioglitazone_component.toml`, `validation/component/sitagliptin_pioglitazone_component_source_benchmark.toml`, `src/bias_nma_adv/component_benchmark.py`, `scripts/verify_component_sources.py`, and `tests/test_component_benchmark.py` | Registers one source-backed sitagliptin/pioglitazone factorial smoke benchmark; blocks broad CNMA, same-trial covariance, and `netmeta` parity claims |
| DTA prototype and source coverage gate | Static source/model coverage check plus algorithmic fixture and source-table validation | `src/bias_nma_adv/dta.py`, `validation/dta_source_coverage.toml`, `validation/dta/`, `validation/reference_runs/dta_mada_reitsma_reference.toml`, `validation/reference_runs/dta_mada_reitsma_midkine_source_reference.toml`, and DTA tests | Implements a nonclinical bivariate DTA prototype, `mada::reitsma` fixture adapter, and one source-backed open-access DTA 2x2 benchmark; blocks broad DTA real-data validation, HSROC parity, and diagnostic-accuracy claims |
| ML-NMR source coverage gate | Static fail-closed source/readiness check plus bounded source-search audits | `src/bias_nma_adv/mlnmr_coverage.py`, `validation/mlnmr_source_coverage.toml`, `validation/mlnmr_source_search_2026_07_17.toml`, `validation/mlnmr_source_search_2026_07_18.toml`, and ML-NMR coverage tests | Records that no admissible public trial-IPD plus aggregate covariate ML-NMR benchmark exists yet; blocks real ML-NMR validation and `multinma` parity claims |
| Improvement review ledger | Static milestone review plus validator | `validation/reviews/improvement_review_2026_07_18.toml`, `src/bias_nma_adv/improvement_review.py`, and `tests/test_improvement_review.py` | Records that the current governance/polish milestone passed role-based review while the global world-class objective remains incomplete |
| Simulation matrix | Static simulation matrix plus dynamic smoke run | `validation/simulation_matrix.toml`, `src/bias_nma_adv/simulation_matrix.py`, `scripts/run_simulation_matrix.py`, and `tests/test_simulation_matrix.py` | Runs executable operating-characteristic smoke checks; uses no real data and cannot certify real clinical performance |
| Portfolio reuse registry | Static repo-name/relative-asset registry plus optional local root scan | `validation/portfolio_reuse_sources.toml`, `src/bias_nma_adv/portfolio_reuse.py`, `scripts/scan_portfolio_reuse.py`, and `tests/test_portfolio_reuse.py` | Records reusable local methods and source-ingestion candidates; scans are non-certifying and do not import evidence |
| Multiperson review ledger | Static review disposition plus validator | `validation/reviews/multiperson_review_2026_07_18.toml`, `src/bias_nma_adv/review_ledger.py`, and `tests/test_review_ledger.py` | Records review findings, actions, and next gates; it is process governance, not validation evidence or model certification |
| Unified validation status | Dynamic local gate summary | `src/bias_nma_adv/validation_status.py`, `scripts/write_validation_status.py`, and `tests/test_validation_status.py` | Composes current gates, including the proof-effect bundle, into JSON; does not certify models or enable clinical/HTA reporting |
| External source-artifact pin gate | Dynamic optional hash verification of local answer-key artifacts | `src/bias_nma_adv/reversal_yardstick.py`, `src/bias_nma_adv/validation_status.py`, `scripts/write_validation_status.py`, `scripts/verify_reversal_source_artifacts.py`, and validation-status tests | Reports unavailable artifacts distinctly and fails closed on hash drift; no local private path is committed |
| Reversal aggregate answer-key runner | Dynamic hash-verified aggregate replay of the external arena JSON | `src/bias_nma_adv/reversal_runner.py`, `scripts/run_reversal_yardstick.py`, and `tests/test_reversal_runner.py` | Verifies aggregate yardstick metrics and blocks certification/global-completion shortcuts; it is not source-backed reversal-case fitting or superiority evidence |
| CI validation workflow | Dynamic GitHub Actions run | `.github/workflows/validation.yml` plus uploaded `validation-status` and `simulation-matrix-report` artifacts | Runs tests and machine-readable gates; a green workflow is execution evidence, not production certification |
| Source-identity snapshots | Dynamic public API check | `validation/source_checks/*source_check.json` | Verifies public-record identity and reachability, not numeric extraction |
| Event-count snapshots | Dynamic public API check | `validation/source_checks/*event_counts.json` | Verifies exact abstract count tokens and nearby treatment terms, not full paper extraction |
| Reported-HR snapshots | Dynamic public API check | `validation/source_checks/*reported_hr_tokens.json` | Verifies HR/CI abstract tokens near a hazard-ratio anchor, not survival model fitting |
| Statistical results | Dynamic | Future benchmark artefacts | No superiority claim is made here without generated benchmark outputs |

## References

- `netmeta`: https://cran.r-project.org/package=netmeta
- `multinma`: https://dmphillippo.github.io/multinma/
- `MBNMAdose`: https://cran.r-project.org/package=MBNMAdose
- `crossnma`: https://cran.r-project.org/package=crossnma
- CINeMA: https://cinema.med.auth.gr/
- ROB-MEN: https://cinema.ispm.unibe.ch/rob-men/
- Wigle A, Beliveau A, Nikolakopoulou A, Lin L. Creating treatment and component hierarchies in component network meta-analysis. arXiv:2605.15142. https://arxiv.org/abs/2605.15142
